# API key is from https://console.anthropic.com/
# payment is per token, and separate from claude code!
ANTHROPIC_API_KEY=

# get from telegram's @BotFather bot
TELEGRAM_BOT_TOKEN=

# need TELEGRAM_CHAT_ID so it knows where to 
# send the forwarded messages.
# it should be a numeric ID, not the bot username. 
# The easiest way to find it: send a message to your bot on Telegram,
# then run:
# curl https://api.telegram.org/bot<your-token>/getUpdates
# Look for "chat":{"id":123456789,...} in the response -
# that number is your chat ID.
TELEGRAM_CHAT_ID=
